name: Update README TOC

on:
  pull_request:
    branches:
      - master
    paths:
      - README.md

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
